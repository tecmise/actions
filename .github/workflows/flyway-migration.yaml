name: Generate Connectors

on:
  workflow_call:
    secrets:
      database_password:
        required: true
      DISCORD_NOTIFIER_URL:
        required: true

    inputs:
      schema:
        description: 'Schema for the database'
        required: true
        type: string
      user:
        description: 'User for the database'
        required: true
        type: string
      endpoint:
        description: 'Endpoint for the connector'
        required: true
        type: string

jobs:
  validate-migrations:
    name: Validating Migrations
    runs-on: ['infrastructure', 's4s', "self-hosted", "linux", "x64", "amazon"]
    outputs:
      is-valid: "${{ steps.validate.outputs.exit_code == '0' }}"
    container:
      image: flyway/flyway:latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Validar SQLs com Flyway
      id: validate
      run: |
        echo "🔍 Iniciando validação..."
        set +e
        VALIDATION_OUTPUT=$(flyway -url="jdbc:postgresql://${{ inputs.endpoint }}/permissions" \
          -user="${{ inputs.user }}" \
          -password="${{ secrets.database_password }}" \
          -schemas="${{ inputs.schema }}" \
          -ignoreMigrationPatterns="*:pending" \
          -connectRetries=60 \
          -locations="filesystem:./migrations" \
          validate 2>&1)
        EXIT_CODE=$?
        echo "$VALIDATION_OUTPUT" > validation.log
        echo "exit_code=$EXIT_CODE" >> $GITHUB_OUTPUT

    - name: Upload Validation Log
      uses: actions/upload-artifact@v4
      with:
        path: validation.log
        name: validation-log

  send-notification-validate:
    name: Send Notification Validate Error
    runs-on: ['infrastructure', 's4s', "self-hosted", "linux", "x64", "amazon"]
    needs: validate-migrations
    if: needs.validate-migrations.outputs.is-valid == 'false'
    steps:
    - name: Download Validation Log
      uses: actions/download-artifact@v4
      with:
        name: validation-log

    - name: Notify Error
      uses: tecmise/actions/.github/actions/discord-notifier@v3.5.0
      with:
        title: '❌  Houve um erro ao validar migrations ❌ '
        message: "Falha na validação das migrations."
        severity: 'error'
        discord-webhook-url: "${{ secrets.DISCORD_NOTIFIER_URL }}"

    - name: Send Log File
      run: |
        curl -X POST ${{ secrets.DISCORD_NOTIFIER_URL }} -F "file=@validation.log"


  migrate:
    name: Migrating Database
    runs-on: ['infrastructure', 's4s', "self-hosted", "linux", "x64", "amazon"]
    if: needs.validate-migrations.outputs.is-valid == 'true'
    needs: validate-migrations
    outputs:
      is-valid: "${{ steps.migrate.outputs.exit_code == '0' }}"
    container:
      image: flyway/flyway:latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Validar SQLs com Flyway
      id: migrate
      run: |
        echo "🚀 Rodando migrações..."
        set +e
        MIGRATION_OUTPUT=$(flyway -url="jdbc:postgresql://${{ inputs.endpoint }}/permissions" \
          -user="${{ inputs.user }}" \
          -password="${{ secrets.database_password }}" \
          -schemas="${{ inputs.schema }}" \
          -ignoreMigrationPatterns="*:pending" \
          -connectRetries=60 \
          -locations="filesystem:./migrations" \
          migrate 2>&1)
        EXIT_CODE=$?
        echo "$MIGRATION_OUTPUT" > migration.log
        echo "exit_code=$EXIT_CODE" >> $GITHUB_OUTPUT

    - name: Upload Validation Log
      uses: actions/upload-artifact@v4
      with:
        path: migration.log
        name: migration-log

  send-notification-migrate-error:
    name: Notify Error
    runs-on: ['infrastructure', 's4s', "self-hosted", "linux", "x64", "amazon"]
    needs: migrate
    if: needs.migrate.outputs.is-valid == 'false'
    steps:
    - name: Download Validation Log
      uses: actions/download-artifact@v4
      with:
        name: migration-log

    - name: Notify Error
      uses: tecmise/actions/.github/actions/discord-notifier@v3.5.0
      with:environment:
        title: '❌  Houve um erro ao Executar Migration ❌ '
        message: "Falha ao executar migrations."
        severity: 'error'
        discord-webhook-url: "${{ secrets.DISCORD_NOTIFIER_URL }}"

    - name: Send Log File
      run: |
        curl -X POST ${{ secrets.DISCORD_NOTIFIER_URL }} -F "file=@migration.log"
        

  send-notification-migrate:
    name: Notify
    runs-on: ['infrastructure', 's4s', "self-hosted", "linux", "x64", "amazon"]
    needs: migrate
    if: needs.migrate.outputs.is-valid == 'true'
    steps:
    - name: Download Validation Log
      uses: actions/download-artifact@v4
      with:
        name: migration-log

    - name: Notify Error
      uses: tecmise/actions/.github/actions/discord-notifier@v3.5.0
      with:
        title: '🚀 Migration ${{ github.event.repository.name }} 🚀'
        message: "Migrações aplicadas com sucesso."
        severity: 'info'
        discord-webhook-url: "${{ secrets.DISCORD_NOTIFIER_URL }}"

    - name: Send Log File
      run: |
        curl -X POST ${{ secrets.DISCORD_NOTIFIER_URL }} -F "file=@migration.log"